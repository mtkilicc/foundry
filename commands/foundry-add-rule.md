---
description: Turn one incident or convention into a single rule (policy = law)
argument-hint: "[the rule / mistake to prevent]"
---

Invoke the **foundry** skill, routing to `rules-gen/single-rule`.

- One file/section per concern, ≤50 lines, concrete (do/don't, mapping tables).
- Framework idioms must be context7-verified, never from memory.
- Rules own policy; after the change, grep skills for restatements and remove them.
- Render the rule per enabled target via sync.

Rule / incident: $ARGUMENTS
