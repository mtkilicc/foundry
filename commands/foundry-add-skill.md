---
description: Add or extend ONE master-expert skill (a FE/BE component specialist) and slot it
argument-hint: "[domain or purpose, e.g. 'dashboard charts']"
---

Invoke the **foundry** skill, routing to `skills-gen/single-skill`.

- Slot first (new leaf vs extend existing vs needs layer-manager).
- Author a **master-expert** leaf from `templates/skills/canonical-leaf.md`:
  fill **Mastery** (contract · decisions · gotchas · extension points) and the
  **Evolution self-check**. Anchor ≥1 leaf doc + ≥1 code file.
- Add one row to the parent decision table; run `foundry_sync.py` + check.

Skill purpose: $ARGUMENTS
