---
description: Make skills talented — unique UI/UX identity and per-project enrichment
argument-hint: "[optional: visual direction; leave blank to let Foundry decide]"
---

Invoke the **foundry** skill, routing to `skills-gen/project-talent` (+ `docs-gen/stack-identity`).

- **Greenfield (nothing to mine):** Foundry **decides** the design — invent a
  focal area + palette/type/density fresh each run (no cross-project compare).
- **Brownfield:** mine `components/ui/`, charts, shell, backend app shapes.
- Write `design-identity.md` / `backend-identity.md` + project fingerprint;
  enrich each leaf's UI composition / Backend patterns; run identity check + sync.

Visual direction (optional): $ARGUMENTS
