# Scope by phase — MVP, v1, v2…

Status: empty

> Phases are **agreed with the user** before Foundry generates skills/rules/full
> docs. Foundry agrees **MVP + v1 only** by default. **v2+ are created only when
> the user asks** (`want_v2=yes`) — until then they stay `optional`.

## Phase summary

| phase | status | target | one-line outcome |
|---|---|---|---|
| **MVP** | empty | | |
| **v1** | empty | | |
| v2 | optional | | *only if user asks* |
| v3+ | optional | | |

`status`: `empty` \| `draft` \| `agreed` \| `shipped` \| `optional`

## MVP — minimum viable

<!-- FOUNDRY:TBD required="user" prompt="Smallest slice that proves value. Bullet features only." -->

### In scope

-

### Out of scope (defer to v1+)

-

### Acceptance (MVP is done when…)

-

## v1 — first production-ready release

<!-- FOUNDRY:TBD required="user" prompt="What ships after MVP? User-facing capabilities." -->

### In scope

-

### Out of scope (defer to v2+)

-

### Acceptance

-

## v2 — next major increment (optional — only if the user asks)

<!-- FOUNDRY:TBD optional="true" prompt="Created only when want_v2=yes. What v2 adds on top of v1." -->

### In scope

-

### Acceptance

-

## v3+ (optional — add rows as needed)

<!-- FOUNDRY:TBD required="user" optional="true" prompt="Future phases; leave empty if unknown." -->

| phase | in scope | notes |
|---|---|---|
| v3 | | optional |
| v4 | | optional |

## Phase change log

| date | change | agreed by |
|---|---|---|
| | initial | |
